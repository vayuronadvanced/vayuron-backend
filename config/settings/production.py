"""
Production settings placeholder.
To be implemented: from .base import *  then DEBUG=False, ALLOWED_HOSTS restricted
to the Hostinger VPS domain, SECURE_* headers (SSL redirect, HSTS, secure cookies),
production PostgreSQL credentials from environment, static/media served via Nginx,
Sentry or similar error tracking hook.
"""
