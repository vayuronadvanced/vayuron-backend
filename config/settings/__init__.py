"""
Settings package placeholder.
To be implemented: base.py (shared settings), development.py, production.py, testing.py
each importing from base and overriding per-environment values (DEBUG, ALLOWED_HOSTS,
DATABASES, CORS, static/media storage, etc). DJANGO_SETTINGS_MODULE should point to
one of these per environment.
"""
