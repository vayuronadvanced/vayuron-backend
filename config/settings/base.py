"""
Base settings placeholder — shared across all environments.
To be implemented: INSTALLED_APPS (incl. rest_framework, corsheaders, and all
apps.* listed under backend/apps/), MIDDLEWARE, TEMPLATES, DATABASES (PostgreSQL,
values sourced from environment variables — never hardcoded), REST_FRAMEWORK
config (auth classes, pagination, throttling), AUTH_USER_MODEL, STATIC_URL,
MEDIA_URL, and secret/env loading via django-environ or python-decouple.
"""
